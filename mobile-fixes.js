/* Mobile uses the source page's native section animation and document flow.
   The former script cancelled those animations and manually hid cards by
   scroll progress, which could skip content on short phone viewports. */
